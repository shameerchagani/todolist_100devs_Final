<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>::ToDo List App</title>
    <link rel="stylesheet" href="/public/css/style.css">
    <link href="https://fonts.googleapis.com/css?family=Baloo+Bhai|Candal|Chewy&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;600&display=swap" rel="stylesheet">
</head>
<body>
<header>
    <h1>
        Simple ToDo List App::
    </h1>
</header>
<main>
    <section class="frm-todo">
        <form method="POST" action="/">
        <input type="text" name='addTask' placeholder="Enter a Task ToDo" require autofocus autocomplete="off" />
        <button type="submit" id="btn-add"><span class="fas fa-plus"></span></button>
        </form>
    </section>

    <section class="task-list">
        
            <ul class="todo-list">
                <% todoTasks.forEach(details => { %>
                <li class="todo-list-item" title= "<%= details.date %>">
                <span class="todo-list-item-name"><%= details.addTask %></span>
               <div class="icons">
                <a href="/edit/<%= details._id %>" class="edit" title="Edit Task">
                <span class="fas fa-pen"></span>
                </a>
                <a href="/complete/<%= details._id %>" class="tick" title="Mark Done?">
                    <span class="fas fa-check"></span>
                    </a>
                <a href="/remove/<%= details._id %>" class="remove" title="Remove">
                <span class="fas fa-trash"></span>
                </a>
            </div>
                </li>
                <% }) %>
            </ul>
        
    </section>
</main>
</body>
    
</body>
</html>